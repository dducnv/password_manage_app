import 'package:flutter/material.dart';

class OnboadingMainPage extends StatelessWidget {
  const OnboadingMainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Onboarding"),
      ),
    );
  }
}
